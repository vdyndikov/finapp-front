import React from "react";
import { Text, SafeAreaView } from 'react-native';

export const Costs = props => {
    return (
        <SafeAreaView>
            <Text>Costs</Text>
        </SafeAreaView>
    )
}
